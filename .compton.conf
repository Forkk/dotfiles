# Shadow
shadow = false;

# Fading
fading = false;

# Inactive window dim
inactive-dim = 0.1;
menu-opacity = 1; # Don't dim menus.
focus-exclude = [ "i:a:slock", "name *= 'Firefox'" ];

# Other
backend = "glx";
vsync = "opengl-swc";

# Window type settings
wintypes:
{
  tooltip = { fade = true; shadow = false; opacity = 0.75; focus = true; };
  menu = { fade = false; shadow = false; opacity = 1; focus = true; };
  dropdown_menu = { fade = false; shadow = false; opacity = 1; focus = true; };
  popup_menu = { fade = false; shadow = false; opacity = 1; focus = true; };
  # fade: Fade the particular type of windows.
  # shadow: Give those windows shadow
  # opacity: Default opacity for the type of windows.
  # focus: Whether to always consider windows of this type focused.
};

