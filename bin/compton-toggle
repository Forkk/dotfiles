#!/usr/bin/env bash

NOTIF_ARGS='--icon=dialog-information --urgency=low'

if killall picom; then
    update-notify.sh compositor "Compositor" "Compositing disabled" $NOTIF_ARGS
else
    compton-start
    update-notify.sh compositor "Compositor" "Compositing enabled" $NOTIF_ARGS
fi
