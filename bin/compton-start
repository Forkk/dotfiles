#!/usr/bin/env bash

CFGDIR="$HOME/.config/picom"

# Path to base settings file.
BASE="$CFGDIR/picom.conf"

# Path to local settings file.
LOCAL="$CFGDIR/$HOSTNAME.conf"

# Path to generated config
GEN="$HOME/.cache/picom.conf"

cat $BASE $LOCAL > "$GEN"
picom -b --config "$GEN" "$@"
